#!/bin/sh
#
# This script gives an easy access to DVC CLI.
# For example, you can add this `<project root>/bin` directory to `PATH` environment variable
# so that you can execute `dvc` without `peotry run` nor `poetry shell`.

poetry run dvc "$@"
